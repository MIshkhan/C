#ifndef Number_Groups
#define Number_Groups

#include <vector>
#include <string>
using namespace std;

class NumberGroups{
public:
  void printGroups(vector<int>); // makes groups of numbers by reading int vector
  void printGroups(string);      // makes groups of numbers by reading file 
};

#endif
